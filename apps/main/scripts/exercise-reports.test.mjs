import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';

const root = resolve(import.meta.dirname, '..', '..', '..');
const app = resolve(root, 'apps', 'main', 'src');
const read = (path) => readFileSync(path, 'utf8');
const service = read(resolve(app, 'services', 'exerciseReportsService.ts'));
const reportStatus = read(resolve(app, 'services', 'exerciseReportStatus.ts'));
const practice = read(resolve(app, 'components', 'ExercisePractice', 'ExercisePractice.tsx'));
const dashboard = read(resolve(app, 'components', 'ProblemReports', 'ProblemReportsDashboard.tsx'));
const locationPicker = read(resolve(app, 'components', 'ProblemReports', 'ReportExerciseLocationPicker.tsx'));
const adminBuilder = read(resolve(app, 'components', 'AdminExercises', 'AdminExerciseBuilderPage.tsx'));
const verification = read(resolve(app, 'components', 'ProblemReports', 'AdminExerciseVerification.tsx'));
const generalReport = read(resolve(app, 'components', 'ProblemReports', 'GeneralProblemReportModal.tsx'));
const appSource = read(resolve(app, 'App.tsx'));
const rules = read(resolve(root, 'firestore.rules'));
const indexes = JSON.parse(read(resolve(root, 'firestore.indexes.json')));

assert.match(service, /status:\s*'new'/, 'new reports must use status new');
assert.match(service, /priority:\s*'normal'/, 'new reports must use normal priority');
assert.match(reportStatus, /function isActiveExerciseReport/, 'one active-report helper must define list and count behavior');
assert.match(reportStatus, /function isVisibleExerciseReport/, 'resolved reports need one dashboard-visibility rule');
assert.match(service, /filters\.status === 'active'/, 'the operational queue must explicitly select active reports');
assert.match(service, /isActiveExerciseReport\(\{ status \}\)/, 'the counter must reuse the active-report helper');
assert.match(service, /PAGE_SIZE = 25/, 'reports must be paginated');
assert.match(service, /while \(reports\.length < PAGE_SIZE/, 'client-side filters must scan beyond the first raw Firestore page');
assert.match(service, /recentSubmissions/, 'immediate duplicate submissions must be guarded');
assert.match(practice, /disabled=\{reportSubmitting\}/, 'submit controls must lock while sending');
assert.match(practice, /setReportFormOpen\(true\)/, 'exercise must open a report form');
assert.match(practice, /Reportar problema/, 'report action must remain visible in practice');
assert.match(generalReport, /source: 'hamburger-menu'/, 'general reports must identify their menu origin');
assert.match(generalReport, /initialWorkbookId/, 'general reports should prefill available context');
assert.match(appSource, /setGeneralReportOpen\(true\)/, 'the hamburger menu must open the general report');
assert.doesNotMatch(practice, /setCurrentIdx\([^)]*submitProblemReport/, 'report submission must not advance the exercise');
assert.match(appSource, /isAdmin && \([\s\S]*Relatórios de problemas/, 'menu item must be admin-only');
assert.match(appSource, /pendingProblemReports > 0/, 'pending badge must hide at zero');
assert.match(appSource, /currentSection === SectionType\.PROBLEM_REPORTS && !isAdmin/, 'admin route must have a frontend guard');
assert.match(dashboard, /status:\s*'reviewing'/, 'admin can mark reviewing');
assert.match(dashboard, /status:\s*'resolved'/, 'admin can resolve');
assert.match(dashboard, /status:\s*'dismissed'/, 'admin can dismiss');
assert.match(dashboard, /Reabrir como novo/, 'admin can reopen a closed report');
assert.match(dashboard, /closeAfterSuccess/, 'status transitions close only after the update succeeds');
assert.match(dashboard, /actionInFlightRef/, 'duplicate status requests must be blocked synchronously');
assert.match(dashboard, /copyStatus === 'error'/, 'copy failures must be visible');
assert.match(dashboard, /Pendências ativas/, 'the status filter must default to the active operational list');
assert.match(dashboard, /Com rascunho/, 'the status filter must expose drafts');
assert.match(dashboard, /Com correção publicada/, 'the status filter must expose published corrections');
assert.match(dashboard, /Status da denúncia/, 'report status must be shown separately');
assert.match(dashboard, /Status editorial/, 'editorial status must be shown separately');
assert.match(dashboard, /Este exercício possui um rascunho/, 'discarding a report with a draft must ask what to do');
assert.match(dashboard, /Justificativa administrativa para resolver sem publicar/, 'resolving without publishing must require a reason');
assert.match(dashboard, /Problema marcado como corrigido e removido da lista/, 'manual resolution must visibly leave the queue');
assert.match(dashboard, /O relatório foi removido da lista/, 'successful verification must visibly leave the queue');
assert.match(dashboard, /COURSE_WORKBOOKS/, 'workbook filters must use the complete course catalog');
assert.match(dashboard, /lessonOptions\.map/, 'lesson filters must list catalog lessons');
assert.match(dashboard, /Abrir exercício para verificar/, 'admin must be able to open the exact reported exercise');
assert.match(dashboard, /reportedWorkbookCandidates/, 'verification must recover from an incorrectly recorded workbook number');
assert.match(dashboard, /Editar exercício existente/, 'report correction must expose the local override editor');
assert.match(dashboard, /Novo exercício/, 'report correction must expose separate administrative creation');
assert.match(dashboard, /setEditor\(\{ report, location: resolved\.location/, 'valid reports must open the existing override editor directly');
assert.match(dashboard, /setBuilderInitial\(\{ report, location: resolved\.location, courseId: resolved\.courseId, intent: 'new' \}\)/, 'new-from-report must propagate the complete resolved location into sequence authoring');
assert.doesNotMatch(dashboard, /AdminExerciseCreationModal/, 'new-from-report must not return to the disconnected legacy model');
assert.match(adminBuilder, /intent\?: 'existing' \| 'new'/, 'the active builder must distinguish creation from override editing');
assert.match(locationPicker, /COURSE_WORKBOOKS/, 'manual fallback must read the packaged curriculum registry');
assert.match(locationPicker, /setLessonId\(''\); setDayId\(''\); setExerciseId\(''\)/, 'changing workbook context must clear dependent selectors');
assert.match(locationPicker, /getExerciseEditorialStatuses/, 'manual exercise choices must expose existing editorial state');
assert.match(locationPicker, /type="button"/, 'fallback actions must never submit an enclosing form');
assert.match(dashboard, /Marcar correção pronta para verificar/, 'published corrections must have an explicit verification-ready state');
assert.match(verification, /PracticeSection/, 'verification must render the reported exercise directly');
assert.doesNotMatch(verification, /ExercisePractice/, 'admin verification must not use the progress-writing exercise flow');
assert.match(verification, /Problema não corrigido/, 'verification must support reporting an unsuccessful correction');
assert.match(service, /verificationResult/, 'verification results must persist on the report');
assert.match(dashboard, /Copiar dados do exercício/, 'details must copy exercise data');
assert.match(dashboard, /adminNote/, 'details must edit the admin note');
assert.match(dashboard, /Próxima/, 'dashboard must expose pagination');
assert.match(rules, /match \/exerciseReports\/\{reportId\}/, 'Firestore rules must cover exerciseReports');
assert.match(rules, /allow get, list: if isAdmin\(\)/, 'only admins can read reports');
assert.match(rules, /allow create: if signedIn\(\)/, 'signed-in students can create reports');
assert.match(rules, /request\.resource\.data\.userId == request\.auth\.uid/, 'students can create only owned reports');
assert.match(rules, /request\.resource\.data\.status == 'new'/, 'students cannot choose report status');
assert.match(rules, /request\.resource\.data\.priority == 'normal'/, 'students cannot choose priority');
assert.match(rules, /source in \['exercise-practice', 'hamburger-menu'\]/, 'rules must allow both authorized report entry points');
assert.match(rules, /allow update: if isAdmin\(\)/, 'only admins can update reports');
assert.match(rules, /'verificationResult'/, 'admins must be allowed to save verification outcomes');
assert.match(rules, /allow delete: if false/, 'reports are permanent and cannot be deleted');

const indexFields = indexes.indexes.map((index) => index.fields.map((field) => field.fieldPath).join('+'));
for (const required of ['status+createdAt', 'priority+createdAt', 'workbookId+lessonId+createdAt', 'status+priority+createdAt']) {
  assert.ok(indexFields.includes(required), `missing index ${required}`);
}

const pendingCount = (items) => items.filter((item) => item.status === 'new' || item.status === 'reviewing').length;
const reports = [{ status: 'new' }, { status: 'reviewing' }, { status: 'resolved' }, { status: 'dismissed' }];
assert.equal(pendingCount(reports), 2, 'new and reviewing must count as pending');
reports[0].status = 'reviewing';
assert.equal(pendingCount(reports), 2, 'reviewing remains pending');
reports[0].status = 'resolved';
assert.equal(pendingCount(reports), 1, 'resolved leaves pending count');
reports[1].status = 'dismissed';
assert.equal(pendingCount(reports), 0, 'dismissed leaves pending count');

console.log('Exercise reports regression checks passed.');
