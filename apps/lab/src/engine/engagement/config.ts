// ─── Engagement reward constants ─────────────────────────────────────────────

/** Diamonds awarded for completing one exercise session */
export const DIAMONDS_PER_EXERCISE = 7;

/** Days in one engagement cycle */
export const CYCLE_LENGTH = 7;

/** Score weight: each fire token → N stars */
export const FIRE_WEIGHT = 10;

/** Score weight: each diamond → N stars */
export const DIAMOND_WEIGHT = 1;

/** Penalty: each ice token subtracts N stars */
export const ICE_PENALTY = 5;

/** Maximum history entries kept in localStorage */
export const MAX_HISTORY_DAYS = 90;
